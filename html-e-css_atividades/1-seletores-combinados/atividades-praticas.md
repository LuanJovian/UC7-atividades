# Atividades práticas - Seletores Combinados
### Arquivo de referência: "Aula 26.pptx"
### Assunto: Seletores combinados
### *Status*: em andamento ⏳
---
### Slide 133:
(Arquivos da Aula 20 - 04/03/2026)
1. Considerando os arquivos dos htmls semânticos, faça:
    - 1.1. inserir css usando todos os seletores visto em aula.
    - 1.2. Adicione novas funcionalidades nos htmls semânticos repassados.


### Slide 134:
(Arquivos da Aula 19 - 03/03/2026)
1. Para os arquivos 1, 2 e 3 , faça:
    - A. Use seletor universal.
    - B. Use seletores básicos.
    - C. Use seletores combinados.

### Slide 135:
1. Escreva um seletor CSS que estilize todos os `<p>` dentro de uma `<div>`. 
1. Escreva um seletor que estilize apenas os `<p>` que sejam filhos diretos de uma `<section>`. 
1. Escreva um seletor que estilize o `<p>` que aparece imediatamente após um `<h1>`. 
1. Escreva um seletor que estilize todos os `<p>` que aparecem depois de um `<h2>` no mesmo nível hierárquico. 
1. Escreva um seletor que estilize apenas os `<li>` que são filhos diretos de uma `<ul>`. 
1. Escreva um seletor que estilize todos os `<span>` dentro de um `<article>`, independentemente da profundidade.
1. Escreva um seletor que estilize o primeiro `<p>` logo após qualquer `<div>`. 
1. Escreva um seletor que estilize todos os `<img>` que sejam filhos diretos de uma `<div class="galeria">`.


### Slide 136:
1. Escreva um seletor que estilize apenas o `<p>` que vem imediatamente após um `<h2>`. 
1. Escreva um seletor que estilize todos os `<label>` que aparecem depois de um `<input>` dentro do mesmo formulário. 
1. Escreva um seletor que estilize todos os `<li>` que vêm após o primeiro `<li>` dentro da mesma `<ul>`. 
1. Escreva um seletor que estilize apenas os `<p>` filhos diretos de uma `<div class="card">`. 
1. Escreva um seletor que estilize todos os `<p>` descendentes de uma `<section>`. 
1. Escreva um seletor que estilize todos os elementos `<span>` que sejam irmãos gerais de um `<h3>`. 
1. Escreva um seletor que estilize apenas o elemento que aparece imediatamente após um `<header>`.

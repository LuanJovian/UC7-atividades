# Atividades práticas
# Atividades teóricas
## Referência: 
- Arquivo: ["Aula 13.pptx"](https://docs.google.com/presentation/d/1_i0_q0c1L23zcVQN1zE9oRFsHsStdybT/edit?slide=id.g3933759b401_0_30#slide=id.g3933759b401_0_30)
- Data: 23/02/2026

---
### Slide 25
1. Elabore a estrutura básica de um documento HTML5 contendo as tags `<!DOCTYPE html>`, `<html>`, `<head>`, `<title>` e `<body>`. 
1. Crie uma página HTML que contenha um título principal utilizando `<h1>` e dois subtítulos utilizando `<h2>` e `<h3>`. 
1. Desenvolva um trecho de código HTML que apresente três parágrafos distintos utilizando a tag `<p>`. 
1. Construa um exemplo prático que utilize a tag `<br>` para separar linhas de um mesmo texto. 
1. Implemente um código HTML que utilize a tag `<hr>` para separar duas seções de conteúdo.

---
### Slide 26
6. Crie um documento HTML que contenha um comentário explicando a finalidade de uma seção do código utilizando `<!-- -->`. 
1. Desenvolva uma lista não ordenada contendo pelo menos cinco itens relacionados a funcionalidades de um sistema, utilizando `<ul>` e `<li>`. 
1. Elabore uma lista ordenada que represente as etapas de um processo de deploy, utilizando as tags `<ol>` e `<li>`. 
1. Construa um exemplo de página HTML que contenha um link externo utilizando a tag `<a>` apontando para um site institucional. 
1. Crie um código HTML que apresente uma imagem na página utilizando a tag `<img>` com atributos src e alt.

---
### Slide 27
11. Desenvolva uma página HTML que combine títulos (`<h1>`), parágrafos (`<p>`) e uma linha horizontal (`<hr>`) em uma estrutura organizada. 
1. Elabore um documento HTML que contenha um título na aba do navegador utilizando corretamente a tag `<title>` dentro do `<head>`. 
1. Crie um exemplo prático que integre lista ordenada, parágrafos e títulos em uma mesma página HTML. 
1. Desenvolva uma página simples de apresentação de um sistema (ex: catálogo) contendo: (1) Título da página, (2) Um título principal, (3) Dois parágrafos e (4) Uma imagem.
1. Construa um documento HTML completo que utilize, de forma integrada, as seguintes tags: `<html>`, `<head>`, `<title>`, `<body>`, `<h1>`, `<p>`, `<ul>`, `<li>`, `<a>` e `<img>`, simulando a página inicial de um sistema web. 

---
### Slide 28
16. Desenvolva um documento HTML que contenha: Estrutura completa do HTML5 Um título na aba do navegador Um título principal (`<h1>`) Uma linha horizontal (`<hr>`) Dois parágrafos explicativos sobre um sistema web. 
1. Elabore uma página HTML que apresente um menu simples utilizando lista não ordenada (`<ul>` e `<li>`), contendo pelo menos quatro links (`<a>`) para seções fictícias do site (ex: Início, Produtos, Serviços e Contato). 
1. Construa um código HTML de uma página institucional que inclua: Um comentário explicativo no código (`<!-- -->`) Uma imagem com descrição alternativa (alt) Uma lista ordenada (`<ol>`) com três etapas de funcionamento de um sistema Uso adequado de títulos hierárquicos (`<h1>` e `<h2>`) para organização do conteúdo.

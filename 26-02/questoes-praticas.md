1. Crie uma página HTML com uma estrutura básica contendo `<html>`, `<head>` e `<body>`, e um título principal usando `<h1>`. 
1. Desenvolva um exemplo utilizando uma `<div>` contendo um título e um parágrafo. 
1. Crie um parágrafo que utilize a tag `<span>` para destacar uma palavra no meio do texto. 
1. Construa uma página demonstrando a diferença entre um elemento block e um inline. 
1. Crie uma tabela simples com 2 colunas (Nome e Idade) e 2 linhas de dados. 
1. Desenvolva uma tabela utilizando as tags `<thead>` e `<tbody>`. 
1. Crie um formulário simples contendo um campo de texto para nome e um botão de envio. 
1. Elabore um formulário com os seguintes inputs: nome (text), email (email) e senha (password).

---
9. Crie um formulário que utilize corretamente a tag `<label>` associada aos inputs. 
1. Desenvolva um formulário com um campo de seleção usando `<select>` e pelo menos três `<option>`. 
1. Crie um formulário contendo um campo de mensagem utilizando `<textarea>`. 
1. Elabore uma página HTML acessível contendo uma imagem com atributo alt descritivo. 
1. Crie uma página que utilize corretamente a hierarquia de títulos (h1, h2, h3). 
1. Desenvolva um formulário que contenha um input do tipo checkbox e outro do tipo radio. 
1. Crie uma página HTML completa que contenha: uma `<div>`, um formulário, um input, um label e um select na mesma estrutura.
---
16. Crie um estilo CSS que deixe todos os títulos `<h1>` na cor azul. 
1. Aplique uma cor de fundo em toda a página utilizando CSS. 
1. Defina o tamanho da fonte dos parágrafos (p) para 18px usando CSS. 
1. Crie um CSS que altere a fonte do texto da página para Arial. 
1. Aplique uma borda simples em uma `<div>` usando CSS. 
1. Utilize CSS para centralizar o texto de um título. 
1. Crie uma regra CSS que mude a cor de todos os parágrafos para verde. 
1. Aplique espaçamento interno (padding) em uma `<div>` usando CSS. 
1. Crie um seletor CSS que altere a cor de um elemento específico usando seu id. 
1. Desenvolva um exemplo onde: O HTML cria um título O CSS altera a cor e o tamanho desse título
